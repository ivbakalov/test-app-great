import { ResizeLinksDirective } from './resize-links.directive';

describe('ResizeLinksDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeLinksDirective();
    expect(directive).toBeTruthy();
  });
});
